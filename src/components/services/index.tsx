import { AxiosInstance } from "axios";

export const MainlayoutServices = (axiosInstance: AxiosInstance) => {
  const apiBaseUrlPatients = "harmor/patients";
  // return {
  //   profile: () => {
  //     return axiosInstance
  //       .get(`${apiBaseUrlPatients}`)
  //       .then((result) => result.data);
  //   },
  // };
  return <></>;
};
